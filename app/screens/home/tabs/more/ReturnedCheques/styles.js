import {StyleSheet} from 'react-native';
import {containerColors} from '../../../../../config/theme'

export default StyleSheet.create({
    container: {
        ...containerColors,
        flex: 1
    }
});