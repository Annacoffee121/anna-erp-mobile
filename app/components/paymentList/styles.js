import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    blackText: {
        color: '#000000'
    },
    itemMainView: {
        marginTop: 10,
        flexDirection: 'row',
        justifyContent: 'space-between',
        alignItems: 'center'
    },
    iconView: {
        flexDirection: 'row',
        justifyContent: 'space-between',
        alignItems: 'center'
    }
});